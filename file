1.什么是泛型
    JDK5引入的新特性，为了向下兼容，虚拟机不支持泛型，只有最原始的类型。java中实现的是伪泛型机制
    伪泛型：在编译器进行处理的时候 做了类型转换 强转
          生成字节码的时候 还是原来的那一套原始类型的代码 虚拟机中不需要新增其他类型 或者 新的概念代码

2.什么是泛型擦除机制
    检查泛型类型，获取泛型类型
    擦除泛型类型，并转换为限定类型
       如果没有限定类型，转换为object
       若果有限定类型，就转换为限定类型
       如果有多个限定类型，就选择第一个边界类型作为原始类
    必要时插入类型转换，保证类型安全
    生成桥方法以便在扩展时保持多态性



元注解：注解上的注解
@Target注解 没有指定 则可以放到所有地方
    Type 类上面
    ...

@RetentionPolicy 保留级别
    Source  源码阶段。编译之后就会被抹除  APT注解处理器。一般用于生成额外的辅助类
    Class   由编译器保留，字节码，会被jvm忽略  字节码增强(在字节码中写代码)
    Runtime jvm保留 运行环境可以使用

